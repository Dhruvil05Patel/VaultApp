import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("VaultApp — Setup Complete ✅")
            .font(.title)
            .padding()
    }
}

#Preview {
    ContentView()
}
